
rootProject.name = 'core'
def isIncludedBuild = gradle.parent != null
if(!isIncludedBuild) {
	includeBuild ('../../deviceDaemonLib/'){
		dependencySubstitution {
			substitute module('org.droidmate:deviceDaemonLib') with project(':')
		}
	}
}
