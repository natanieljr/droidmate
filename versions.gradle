project.ext {
	internalTask = null  // use this as .group value of any task to make them invisible from gradle task list

	javaVersion = JavaVersion.VERSION_1_8

	driverLib_version = '2.0-LATEST'  // our device interface (right now subject to change, but after refactoring it should not require further updates)

	kotlin_version = '1.2.71'
	coroutines_version = '0.26.0'
	androidGradlePlugin_version = "3.2.1"
	androidBuildToolsVersion = "28.0.3"
	androidMinSdkVersion = 23
	androidTargetSdkVersion = 25
	androidCompileSdkVersion = 27

	// for some strange konrad.utilities used within misc for configuration??
	utilities_version = 'v0.2.4'
}